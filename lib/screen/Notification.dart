import 'package:flutter/material.dart';

class NotificationLe extends StatelessWidget {
  const NotificationLe({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Notification'),
      ),
    );
  }
}